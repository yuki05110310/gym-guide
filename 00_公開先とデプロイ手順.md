# このページの公開先（最初に読め）

**久保田さんが実際に見ているURL: https://bodytransform.pages.dev/**

Cloudflare Pages。プロジェクト名 `bodytransform`。**GitHub連携なし（Git Provider: No）＝ push しても自動で反映されない。**
`index.html` を直したら、必ず下のコマンドを叩くこと。叩かなければ画面は古いまま。

## デプロイ手順

```bash
cd ~/HQ/P_Personal/肉体改造
mkdir -p /tmp/bt/img
cp index.html /tmp/bt/
cp img/m-*.webp /tmp/bt/img/
wrangler pages deploy /tmp/bt --project-name=bodytransform --branch=main --commit-dirty=true
```

`.md`（このファイル・メニュー・ヒアリング）と `img/IMG_*.webp` は**公開しない**ので、上のように
`index.html` と `img/m-*.webp` だけを一時ディレクトリにまとめてから送る。フォルダごと送るな。

## デプロイ後の確認（省略禁止）

`https://bodytransform.pages.dev/` を実際に開いて、直した文字列がページに入っているかを見る。
「デプロイ成功」のメッセージだけでは反映確認にならない。

## GitHubリポジトリ（バックアップ用・公開先ではない）

`yuki05110310/gym-guide` → `https://yuki05110310.github.io/gym-guide/` でも見えるが、
**久保田さんはこちらを見ていない。**ここだけ更新して「反映しました」と言うのは間違い（2026-08-13にやらかした）。
ソース管理としては push しておく。

## 過去の事故

- 2026-08-13: `.md` だけ直して `index.html` を3日放置 → 画面が古いまま
- 2026-08-13: `index.html` を直したが push せず → 画面が古いまま
- 2026-08-13: GitHubに push したが Cloudflare にデプロイせず → 画面が古いまま（本当の公開先を確認していなかった）
